/**
 * Feature flags do CRICRI React
 */
export const FEATURES = {
  /** MEOW — diferencial do game */
  meow: true,
  /** Cri Cabrunco (tamagotchi) */
  tamagotchi: true,
  /** SFX Web Audio */
  sfx: true,
} as const;
